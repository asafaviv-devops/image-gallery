apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app

resources:
- ../../base

namePrefix: dev-

commonLabels:
  environment: dev

patches:
- path: deployment-patch.yaml
  target:
    kind: Deployment
    name: image-gallery

configMapGenerator:
- name: image-gallery-config
  behavior: merge
  literals:
  - LOG_LEVEL=DEBUG
  - S3_BUCKET=image-gallery-dev-bucket
