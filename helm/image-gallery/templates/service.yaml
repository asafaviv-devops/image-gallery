apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-gallery.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "image-gallery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-gallery.selectorLabels" . | nindent 4 }}
